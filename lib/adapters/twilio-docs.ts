import { AssetAdapter } from "./types";

// Informational only — no runtime registry yet.
export const twilioDocsAdapter: AssetAdapter = {
  sourceType: "twilio-docs",
  displayName: "Twilio Docs",
  enabled: true,
};
